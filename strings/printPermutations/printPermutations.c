#include <stdio.h>

void main()
{
    int i, j;
    char word[10], key, swap;

    printf("Enter any word.");
    scanf("%d",word);

    for (i = 0; word[i] != '\0'; i++)
    {
        for (j = i + 1; word[j] != '\0'; j++)
        {

        }
        
    }
    
}